# Publication connector — server-only (api-internal / publication worker).
connectorId: 'publication'
generate:
  adminNodeSdk:
    outputDir: '../../../../../../packages/data-access/generated/publication'
    package: '@blap/sql-connect-publication'
