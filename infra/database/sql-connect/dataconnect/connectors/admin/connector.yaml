# Admin connector — server-only (admin app via Admin SDK + IAP).
connectorId: 'admin'
generate:
  adminNodeSdk:
    outputDir: '../../../../../../packages/data-access/generated/admin'
    package: '@repo/sql-connect-admin'
