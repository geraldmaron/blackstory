# House lock v1

Admin-visible slot for the versioned house lock scan. Design drops `plate.scan.jpg` or `plate.scan.png` here (and in `brand/cover-lock/v1`).

The lock is the scan. Cite it as `cover-lock/v1`.
