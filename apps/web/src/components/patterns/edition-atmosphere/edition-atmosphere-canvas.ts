/**
 * Class-name helpers for the shared v6 edition atmosphere canvas (grain + grid +
 * optional gutter mosaic). Home, about, and stories routes compose this with their edition root.
 */

export const EDITION_ATMOSPHERE_CANVAS_CLASS = 'ds-edition-atmosphere-canvas';

export function editionAtmosphereCanvasClassName(): string {
  return EDITION_ATMOSPHERE_CANVAS_CLASS;
}
