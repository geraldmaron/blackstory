/**
 * Public barrel for the BB-054 "why this appears" surface — the single import path a parent
 * entity-page mount should use.
 */
export { WhyThisAppears, type WhyThisAppearsProps } from './WhyThisAppears';
