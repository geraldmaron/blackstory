/**
 * Handoffs that already pointed at `/explore` still do. `/` is the Door (reading);
 * the Atlas instrument lives only on `/explore`. `?atlas=1` is not a second door.
 */
export const ATLAS_INSTRUMENT_HREF = '/explore';
