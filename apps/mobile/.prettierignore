# Scoped to apps/mobile, which is excluded from the root pnpm workspace and root
# .prettierignore (see pnpm-workspace.yaml `!apps/mobile` and root .prettierignore) and
# formats itself via `npm run format` / `format:check` in this package.json. Keep in step
# with apps/mobile/.gitignore for generated/build output.

node_modules
package-lock.json

# Expo
.expo
dist
web-build
expo-env.d.ts

# Native (generated, gitignored)
ios
android
.kotlin

# Brand tokens generated by scripts/generate-brand-tokens.ts, which writes its own raw
# serialization (double quotes, no trailing commas) without running Prettier. These files must
# byte-match the generator's committed output (see src/ui/tokens/__tests__/no-drift.test.ts) —
# reformatting them here would only make that drift check fail on the next `tokens:generate` run.
src/ui/tokens/generated

# typescript
*.tsbuildinfo

# docs stay unformatted repo-wide (see root .prettierignore)
*.md
