/**
 * Stories tab index — curated home with featured story band and compact archive index.
 * Replaces the prior flat ListRow section dump (History, Topics, Myths, Methodology).
 */
import { StoriesHomeScreen } from '@/features/learn';

export default function LearnScreen() {
  return <StoriesHomeScreen />;
}
