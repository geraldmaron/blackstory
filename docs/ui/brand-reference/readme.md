# Superseded: v2 brand reference (historical)

The PNGs in this directory are the reference sheet from the owner-supplied
v2 brand package (2026-07-17), kept for historical provenance alongside its
original `source-package-README.txt`. **They are SUPERSEDED by
`brand-system/` "Black Book Brand Identity System 3.0.0-final" (BB-096)**
and must not be used as a source for any new asset, measurement, or color
value.

Use instead:

- `brand-system/assets/` — checksummed vector masters, PNG exports, icons,
  and social assets (web-served copies at `apps/web/public/brand/`)
- `brand-system/guide/black-book-brand-guide-final.pdf` — the standards
  guide
- `brand-system/tokens/` — binding CSS/JSON tokens
- `docs/ui/brand.md` — the usage contract

Notable differences from these v2 PNGs: the v3 lockup is outlined artwork
with the symbol as the first B (visible text begins "lack Book" — no typed
second B), the light canvas is Archive Paper `#F4EFE5` rather than white,
and the display face is Inter Display rather than Sora. Do not delete the
PNGs — they document what BB-068 shipped.
