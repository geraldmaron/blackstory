# How it works hub

**Status:** binding pattern for the trust hub (2026-09-15).  
**Route:** `/how-it-works`  
**Legacy:** `/apparatus` 308s here. Section deep links: `/about`, `/methodology`, `/data`, `/law`, `/books`, `/lives` → `?s=`.

## Intent

One human-named room for how the archive works. Stories stays a **plain primary-axis link** (no Stories disclosure). Rooms remains the only command-bar disclosure and lists this hub once under How it decides.

## Chapters

| Section id | Job |
|---|---|
| `about` | What this is and refuses to do |
| `methodology` | How a record gets in |
| `data` | Presence across time (see Data arc below) |
| `law` | Landmark statutes + link to `/law/browse` |
| `books` | Sample challenged titles + link to `/books/browse` |

Shared shell: `ds-hub-toc`, `ds-hub-section`, act label, `GroupHeading`, `Prose`. Do not invent a second visual dialect per chapter.

## Data chapter arc

1. **Counted** — Census population; `PopulationDecadeSpine` is the share path that opens the time spine.
2. **Lived** (`#lives`) — Continues that spine; `LivesClassArcChart` then decade rail / panel. Laws beside figures, never as cause (P-02).
3. **Measured gaps** — Wealth, housing, justice agency series; `GapMagnitudeSight` for true ratios; existing DataFigure charts.
4. **How to read** — Three rules + Methodology / Stories CTAs.

Evidence apparatus in v10 product thesis means the **trust layer**, not this URL.

## Chrome

- Command bar: Map | Stories | Records | Rooms ▾ (How it works inside Rooms).
- Mobile More: one How it works row (web hub), not peer About/Methodology/Law/Data/Books rows.
